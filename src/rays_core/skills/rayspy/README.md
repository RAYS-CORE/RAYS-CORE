# RAYSpy

## Investigation Pipeline (MCP)

A multi-agent OSINT investigation pipeline lives at [`mcp/`](mcp/README.md),
exposed as a single MCP tool (`rays_investigate`) for host agents like
Cursor or Claude Desktop. It's a standalone package - see its README for
architecture, setup, and current stub-tool status.
